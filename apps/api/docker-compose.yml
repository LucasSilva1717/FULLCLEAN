version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: fullclean-postgres
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_DB: fullclean
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: